# Base de datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=deliverydeagua
DB_PORT=3306

# JWT
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui

# Servidor
PORT=8001
NODE_ENV=development

# /startnow — código de verificación (HMAC); en producción usar valor secreto propio
# STARTNOW_CODE_PHRASE=Crea tu empresa ahora

# SMTP (obligatorio en producción para enviar el código por email)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="AquaDelivery <noreply@example.com>"

# WhatsApp para pedir el código mientras no haya mail (solo dígitos, sin +)
# STARTNOW_WHATSAPP_NUMBER=5492924406159
