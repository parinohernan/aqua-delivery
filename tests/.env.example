# Copiar a .env y completar con tu token.
# cp .env.example .env
#
# API del backend (puerto por defecto 8001)
API_BASE_URL=http://localhost:8001/api
#
# Token JWT: obtenerlo tras hacer login en la app (DevTools > Network > request a /api/ > header Authorization)
TEST_TOKEN=
